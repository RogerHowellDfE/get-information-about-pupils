@model GlossaryViewModel

<table class="govuk-table">
    <tbody class="govuk-table__body" id="help--metadata-files--table">
        @for (var i = 0; i < Model.MetaDataDownloadList.Count(); i++)

        {
            <tr class="govuk-table__row">
                <td class="govuk-table__cell" id="downloads--downloads-data-name--text-@i">@Html.Raw(Model.MetaDataDownloadList[i].Name)</td>
                <td class="govuk-table__cell" id="downloads--downloads-data-date--text-@i">@Html.Raw(Model.MetaDataDownloadList[i].Date.ToString("dd/MM/yyyy", CultureInfo.InvariantCulture))</td>
                <td class="govuk-table__cell">
                    <a asp-action="GetBulkUploadTemplateFile" asp-controller="Glossary" asp-route-name="@Html.Raw(Model.MetaDataDownloadList[i].FileName)" class="downloads--downloads-data-link--link govuk-link" id="downloads--downloads-data-link--link-@i">@Html.Raw(Model.MetaDataDownloadList[i].FileName)</a>
                </td>
            </tr>
        }
    </tbody>
</table>