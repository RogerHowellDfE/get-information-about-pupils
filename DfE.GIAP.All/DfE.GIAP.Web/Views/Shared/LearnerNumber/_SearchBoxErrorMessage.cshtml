@model LearnerNumberSearchViewModel

@if (!ViewData.ModelState.IsValid)
{
    <span id="search--errormessage" class="govuk-error-message">
        <span class="govuk-visually-hidden">Error:</span> <span>@Model.SearchBoxErrorMessage</span>
    </span>
}
